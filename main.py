import max_profit as mp
import prime_number as pn

if __name__ == '__main__':
    pn.is_prime_number(1)
    pn.is_prime_number(2)
    pn.is_prime_number(3)
    pn.is_prime_number(4)
    pn.is_prime_number(5)